---
quick_id: 260514-93y
mode: quick
description: Add homepage preloading animation without changing hero section
---

# Quick Task 260514-93y Plan

## Goal

Add a homepage-only preloading overlay that runs before the existing page is revealed:
1. Start with a full white overlay.
2. Fade in large `QUASAR PORTFOLIO` text.
3. Split the title left/right while a centered image appears at the same visual height as the text.
4. Pause briefly, then scale that image until it fills the viewport.
5. Fade/remove the overlay and leave the existing hero section unchanged.

## Task 1 — Add preloader component

**Files**
- `src/components/sections/home/home-preloader.tsx` (new)

**Action**
- Create a client component using CSS animations only.
- Use `content.heroImagePath` as the preloader image source so the image matches the page background asset, without modifying `HeroSection`.
- Keep overlay fixed, white, high z-index, pointer-blocking while active.
- Use semantic hidden text for the complete brand label and visual split spans for animation.

**Verify**
- Overlay starts white and covers the page.
- Text fades/scales into view.
- Left/right title halves split apart.
- Image appears between text halves with height tied to the title size.
- Image scales to full viewport before overlay exits.

**Done**
- Component is self-contained and does not mutate hero styles or content.

## Task 2 — Mount preloader on homepage only

**Files**
- `src/components/sections/home/home-page-client.tsx`

**Action**
- Import and render `HomePreloader` inside `HomePageClient` above the main page content.
- Pass `content.heroImagePath` to the preloader.
- Do not edit `HeroSection` except if a type/interface requires it; expected no changes.

**Verify**
- Homepage shows preloader on page load.
- Existing hero background and hero layout remain as-is after preloader completes.

**Done**
- Preloader is homepage-scoped and independent from hero rendering.

## Task 3 — Validate build and inspect diff

**Files**
- `package.json` scripts
- Git diff

**Action**
- Run available lint/build checks.
- Inspect changed files to confirm only preloader mounting/component and planning artifacts changed.

**Verify**
- Checks pass.
- No unrelated hero changes.

**Done**
- Summary records implementation and verification status.
