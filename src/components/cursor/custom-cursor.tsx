'use client';

// Water ripple effect is now handled per-image via WaterRippleImage component.
// This component is kept as a no-op export so layout.tsx does not break.
export function CustomCursor() {
  return null;
}
